Service:

  # Scale-to-zero proxy (pgAdmin). When it is down everything it fronts is
  # stranded: the containers stay stopped AND nothing can wake them.
  sablier:
    deployment_type: essential
    location_type: stationary
    units: "8"
    instances: 1
    restart_when_stopped: true
